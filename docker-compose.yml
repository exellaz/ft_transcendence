services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    working_dir: /app
    depends_on:
      - backend
    networks:
      - appnet
  backend:
    build: ./backend
    ports:
      - "4242:4242"
    working_dir: /app
    volumes:
      - ./backend/room.db:/app/room.db
    networks:
      - appnet
networks:
  appnet:
    driver: bridge
